# Remote state for the foundation stage. Uses S3 native locking (use_lockfile);
# no DynamoDB lock table is required.
bucket       = "replace-with-globally-unique-state-bucket"
key          = "mssp/foundation.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
