# Remote state for the activate stage. Uses S3 native locking (use_lockfile);
# no DynamoDB lock table is required.
bucket       = "replace-with-globally-unique-state-bucket"
key          = "mssp/activation.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
