# Optional: configure remote state for activate stage
bucket         = "my-tf-state-bucket"
key            = "mssp/acme/activate.tfstate"
region         = "us-east-1"
dynamodb_table = "terraform-state-locks"
encrypt        = true
