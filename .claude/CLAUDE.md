# CLAUDE.md

Claude Code project instructions live in `../AGENTS.md`.

When working in this repository:
- read `AGENTS.md` first
- follow `README.md` and `docs/` for AWS/ECS/ECR deployment details
- treat `.env` as the primary runtime configuration surface
- do not commit local secrets, Terraform state, or generated CMS config files

This file exists so Claude can discover the repo instructions while the canonical guidance remains in `AGENTS.md`.
